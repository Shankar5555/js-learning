

var accountPassword = "12345" // not to use var becs block and 
accountCity = "Pune"   // not use as that method

const accountId=1445223
// accountId = 2  // not allowed to change const values

let accountEmail = "jmsgavit@gmail.com"

let accountState;   // undefined shows

console.log(accountId);
console.log([accountEmail,accountPassword,accountCity,accountState]);
